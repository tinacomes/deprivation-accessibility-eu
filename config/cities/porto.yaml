# Tier-1 city (generated by `depacc make-city`).
# Facilities via Overpass; travel times via r5 over the country
# Geofabrik extract, clipped to the FUA window (methods.md §5).
# Substitute a tighter regional extract when one exists — see
# hamburg.yaml for the pattern.
city:
  id: "porto"
  name: "Porto"
  country: "PT"
  tier: 1
  fua_code: "PT002F"
sources:
  facilities: "overpass"
  osm_pbf:
    url: "https://download.geofabrik.de/europe/portugal-latest.osm.pbf"
    licence: "ODbL 1.0"
routing:
  engine: "r5"
  modes: ["walk", "car"]
  reverse_direction: ["car", "walk"]
